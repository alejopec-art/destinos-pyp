/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: '#3B82F6', // Blue-500 equivalent, can adjust based on photo
        secondary: '#1F2937', // Gray-800
        accent: '#D4AF37', // Gold-ish color for details
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'], // Modern sans-serif
      },
      animation: {
        'infinite-scroll': 'infinite-scroll 40s linear infinite',
      },
      keyframes: {
        'infinite-scroll': {
          from: { transform: 'translateX(0)' },
          to: { transform: 'translateX(-100%)' },
        }
      }
    },
  },
  plugins: [],
}
